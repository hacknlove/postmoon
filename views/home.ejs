<%- include('header') %>

<header>
    PostMoon
</header>

<h3>Tests:</h3>
<% if (test.length) { %>
   <ul class="testLists">
   <% test.forEach( (e) => { %>
      <li>
        <a href="/<%= e %>">
          <%= e %>
        </a>
    </li>
    <% }) %>
  </ul>
  <% } else { %>
    <div class="informe">
      <h1>Write some tests</h1>
    </div>
  <% } %>


<%- include('footer') %>
